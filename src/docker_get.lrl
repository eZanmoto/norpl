import 'proc';

let $script_commit_sha = '93d2499759296ac1f9c510605fef85052a2c32be';

let [$version, $ok] = $proc.env.[VERSION];
if !$ok {
    $version = '';
}
$print($version);

$version = $version->strip_prefix('v');
$print($version);
# VERSION="${VERSION#v}"

# --- 0. Implement prototype namespace for strings
# 1. Add support for Expr::PrototypeCall
# --- 2. Implement strip prefix
